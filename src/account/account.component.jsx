import { useContext } from "react";
import { UsersContext } from '../contexts/account.context';

const Account = () => {
    // const { users } = useContext(UsersContext);
    return (
        <div>
            {/* {users.map(({ id, f_name }) => (
                <div key={id}>
                    <h2>{f_name}</h2>
                </div>
            ))} */}
        </div>
    )
}


export default Account;